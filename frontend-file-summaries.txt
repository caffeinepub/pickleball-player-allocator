{
  "frontend/src/index.css": "Global styles with a sporty green/yellow pickleball theme using OKLCH color tokens, custom fonts (Inter + Outfit), court pattern utilities, and mobile-first base styles for both light and dark modes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with pickleball color palette (green/yellow), Outfit/Inter font families, custom border radii, card shadows, and animation keyframes for smooth mobile interactions.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interaction; adds `useSearchPlayers` query hook (debounce-ready, calls `searchPlayersByName`) and `useHostAddPlayer` mutation hook (calls `addPlayerToSession` with a principal from a search result), alongside all existing session, profile, messaging, and guest-player hooks.",
  "frontend/src/lib/utils.ts": "Utility functions for class merging, clipboard copy, proprietary rating formatting (formatRating replacing formatDuprRating), and principal shortening.",
  "frontend/src/lib/storage.ts": "LocalStorage helpers for player profiles and sessions; adds `clearInvalidSession(sessionId)` which removes stale session data from localStorage when a SessionNotFound error is confirmed, preventing repeated failed load attempts.",
  "frontend/src/components/Layout.tsx": "Mobile-optimized layout with sticky header showing \"The Ball Club\" text branding (no logo), navigation icons for Messages/History/Profile/Logout for authenticated users, and branded footer.",
  "frontend/src/components/SessionCodeDisplay.tsx": "Session code display component that accepts an explicit `sessionCode` prop (falling back to `sessionId`) and shows it without any length truncation, with copy and share buttons.",
  "frontend/src/components/CourtAssignmentCard.tsx": "Court assignment card accepting `court: bigint`, `teamA: string[]`, `teamB: string[]` props; renders guest players with amber styling and Ghost icon via the `guestPlayerIds` prop, with optional score entry and submitted score display.",
  "frontend/src/components/WaitlistPanel.tsx": "Waitlist panel that detects guest players by checking if the player ID starts with \"guest-\" and renders them with a Ghost icon and amber-styled \"Guest\" badge alongside their display name.",
  "frontend/src/components/GameScheduleInfo.tsx": "Game schedule summary card showing session time, max games per court, and total games calculated from totalMinutes/gameDurationMinutes/courts.",
  "frontend/src/components/MatchResultSubmission.tsx": "Match result card with format selector and Team A/B win buttons; accepts `court: bigint` and optional `guestPlayerIds` to render guest names with Ghost icons and amber styling.",
  "frontend/src/components/PlayerStatsCard.tsx": "Player stats card showing avatar, name, proprietary Rating badge (formatted via formatRating), and win/loss/win-rate statistics; exports both named and default.",
  "frontend/src/components/SessionConfigForm.tsx": "Game configuration form with a new game type selector (4 options: Random Allotment, Round Robin, Ladder League, King/Queen of the Court using icon cards) and a Ranked/Unranked toggle switch; both fields are included in `SessionConfigFormFields` and required before submission.",
  "frontend/src/pages/Home.tsx": "Home page with \"The Ball Club\" welcome, active session chip, host/join action cards, and quick links to Messages, Game History, and Profile (Messages/History hidden for guests).",
  "frontend/src/pages/ProfileSetup.tsx": "Profile creation page that saves guest profiles via createGuestProfile backend call AND persists to localStorage, fixing the guest profile persistence bug.",
  "frontend/src/pages/ProfileView.tsx": "Profile view/edit page that loads from backend with localStorage fallback, includes a \"View Game History\" button for authenticated users.",
  "frontend/src/pages/CreateSession.tsx": "Game creation page that navigates to /host/$sessionId (matching the updated route) after successfully creating a session.",
  "frontend/src/pages/JoinSession.tsx": "Join game page that normalizes the game code (trim + uppercase) before calling `useJoinSession`, correctly uses the returned `SessionId` string for navigation to `/session/$sessionId`, and displays descriptive error messages from the backend on failure.",
  "frontend/src/pages/HostSessionDashboard.tsx": "Host session dashboard with full session management; fixes all TypeScript prop errors by using the correct component interfaces (`submittedTeamAScore`/`submittedTeamBScore` for CourtAssignmentCard, `waitlist` for WaitlistPanel, `onSubmit` for MatchResultSubmission, and `scheduledRounds`/`playerNamesList`/`currentRound` for AllRoundsSchedule), and includes the name-search \"Add Player Manually\" feature for all game types.",
  "frontend/src/pages/PlayerSessionView.tsx": "Player-facing session view; fixes `generateSchedule` → `generateFairSchedule` import and uses index-based scheduler with proper name resolution for both registered and guest players across courts, waitlist, and schedule tabs.",
  "frontend/src/App.tsx": "Main app router with 12 routes using the correct path structure (/host/$sessionId for host dashboard, /session/$sessionId for player view) and all new feature routes registered.",
  "frontend/src/components/AllRoundsSchedule.tsx": "Schedule accordion component rendering all rounds with court assignments and waitlists; updated to pass `court` as `bigint` and split `playerIndices` into `teamA`/`teamB` string arrays matching the updated `CourtAssignmentCard` interface.",
  "frontend/src/pages/Login.tsx": "Login page rebranded to \"The Ball Club\" with text-only branding (no logo image), Internet Identity sign-in and guest continue options.",
  "frontend/src/lib/errorHandling.ts": "Error handling utilities extended with `isDuplicatePlayerError`, `isInvalidPrincipalError`, `isHostOnlyError`, and `formatAddPlayerError` helpers for add-player-specific error cases, alongside the existing session/network/auth error utilities.",
  "frontend/src/lib/scheduler.ts": "New client-side fair round-robin scheduler that generates proper per-round court assignments and waitlists using player indices, ensuring waitlist players get priority in the next round and every player gets equal playing time across all rounds.",
  "frontend/index.html": "HTML entry point with updated title \"The Ball Club\".",
  "frontend/src/components/LoadingGame.tsx": "Animated pickleball mini-game loading screen using Canvas API with two AI-controlled paddles and a bouncing pickleball, shown during loading states.",
  "frontend/src/pages/PublicProfile.tsx": "Public player profile page showing name, bio, work field, and win/loss stats (no mobile number or rating), with a Message button for authenticated non-self viewers.",
  "frontend/src/pages/GameHistory.tsx": "Game history page; fixes the spread type error by explicitly typing `matches` as `CompletedMatch[]` (already unwrapped by the hook) before sorting, and renders match cards with win/loss badges, scores, opponents, and dates.",
  "frontend/src/pages/MessagesInbox.tsx": "Messages inbox page listing all conversation threads sorted by most recent message, with participant preview and timestamp.",
  "frontend/src/pages/ConversationThread.tsx": "Full conversation thread page with chat-style message layout, optimistic message sending, auto-scroll to bottom, and Enter-to-send support."
}
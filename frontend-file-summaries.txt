{
  "frontend/src/index.css": "Global styles with a sporty green/yellow pickleball theme using OKLCH color tokens, custom fonts (Inter + Outfit), court pattern utilities, and mobile-first base styles for both light and dark modes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with pickleball color palette (green/yellow), Outfit/Inter font families, custom border radii, card shadows, and animation keyframes for smooth mobile interactions.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations; restores `useAddPlayerToSession`, fixes `useJoinSession` to accept a plain `string` sessionId, fixes `useCreateSession` with actor-readiness checks and proper `bigint` conversion, and keeps `useGetCallerUserProfile` guest-safe.",
  "frontend/src/lib/utils.ts": "Utility functions for class merging, clipboard copy, DUPR rating formatting (formatDuprRating), and principal shortening; skill level and max games utilities removed.",
  "frontend/src/lib/storage.ts": "LocalStorage helpers for player profile, current session (`StoredSession` with `sessionId`, `isHost`, optional `hostPrincipal`/`courts`), and session-scoped player name registry by index; all operations have try/catch error handling with non-fatal fallbacks and backward-compatible aliases.",
  "frontend/src/components/Layout.tsx": "Mobile-optimized layout wrapper with sticky header, scrollable main, and branded footer; `AppHeader` now includes a logout button that clears the `sessionStorage` auth choice, calls `clear()` on Internet Identity, clears the React Query cache, and navigates to `/login`.",
  "frontend/src/components/SessionCodeDisplay.tsx": "Reusable session code display card with large typography, copy-to-clipboard button, and native share API support with toast feedback.",
  "frontend/src/components/CourtAssignmentCard.tsx": "Court assignment card accepting pre-resolved player name strings, showing Team A vs Team B slots with avatar initials, highlight for current player, and empty slot indicators.",
  "frontend/src/components/WaitlistPanel.tsx": "Waitlist panel displaying queued players by name with position numbers, highlighting the current player, and an empty state; accepts resolved name objects instead of raw principals.",
  "frontend/src/components/GameScheduleInfo.tsx": "Game schedule summary card showing session time, max games per court, and total games calculated from totalMinutes/gameDurationMinutes/courts.",
  "frontend/src/components/MatchResultSubmission.tsx": "Match result card with Team A/B win buttons; refactored to accept `onSubmit`/`isLoading`/`onSkip` props and imports `GameOutcome` as a value directly from `../backend` (not re-exported as type).",
  "frontend/src/components/PlayerStatsCard.tsx": "Player stats card showing avatar, name, DUPR rating badge (formatted via formatDuprRating), and win/loss/win-rate statistics; exports both named and default.",
  "frontend/src/components/SessionConfigForm.tsx": "Session configuration form with a courts stepper (1–10), loading state that disables controls and shows a spinner on the submit button, and proper form submission that calls `onSubmit` with the selected court count.",
  "frontend/src/pages/Home.tsx": "Home page with hero banner, active session resume card (using `currentSession.isHost` which is now correctly typed on `StoredSession`), profile card, and Host/Join action buttons.",
  "frontend/src/pages/ProfileSetup.tsx": "Profile creation page that checks authentication status before calling the backend — authenticated users save to both backend and localStorage, while guest users save to localStorage only with a friendly notice; uses `formatErrorMessage` for user-friendly error display and a local `isSaving` state to track the full async operation.",
  "frontend/src/pages/ProfileView.tsx": "Profile view/edit page; fixes `null` → `undefined` for `duprRating` when saving profile to match `StoredPlayerProfile` type.",
  "frontend/src/pages/CreateSession.tsx": "Session creation page using named imports for `Layout` and `SessionCodeDisplay`, stores session with `isHost: true` via `setCurrentSession`, and navigates to the host dashboard using the correct typed route `/session/$sessionId/host` with `params`.",
  "frontend/src/pages/JoinSession.tsx": "Join session page that passes a plain string to `joinSession.mutateAsync`, stores session with `isHost: false` via `setCurrentSession`, and shows user-friendly error messages via `formatErrorMessage`.",
  "frontend/src/pages/HostSessionDashboard.tsx": "Host dashboard that resolves player names by looking up each player's Principal ID in `sessionState.players` (the authoritative index), fixing the off-by-one bug where the host at index 0 was unaccounted for; `nextIndex` for new players now uses `sessionState.players.length` to correctly align with the backend's players array.",
  "frontend/src/pages/PlayerSessionView.tsx": "Player session view that imports `getSessionPlayerNames` from the restored storage module, resolves player names by Principal ID index lookup, and renders court assignments, waitlist, match result submission, and schedule tabs.",
  "frontend/src/App.tsx": "Main app router with 8 routes; adds a `/login` route (no guard) and applies a `beforeLoad` guard to all other routes that redirects unauthenticated/unchoiced users to `/login` via `sessionStorage` check.",
  "frontend/src/components/AllRoundsSchedule.tsx": "Collapsible accordion showing all planned rounds; now accepts `sessionPlayers: PlayerId[]` and resolves each slot's name by finding the player's index in that array, replacing the fragile rotation-shift algorithm with a direct ID-to-name lookup that guarantees count integrity.",
  "frontend/src/pages/Login.tsx": "New Login page with Internet Identity sign-in and \"Continue as Guest\" options, featuring the pickleball court background, app logo, and two action cards; exports helper functions (`setAuthChoice`, `getAuthChoice`, `clearAuthChoice`) for managing the session-scoped auth choice flag in `sessionStorage`.",
  "frontend/src/lib/errorHandling.ts": "Error handling utilities with `isCanisterStoppedError`, `isAuthError`, `isNetworkError`, `formatErrorMessage` (covering all common IC/session error cases), and `retryWithBackoff` with exponential backoff for canister-stopped errors."
}
{
  "frontend/src/index.css": "Global styles with a sporty green/yellow pickleball theme using OKLCH color tokens, custom fonts (Inter + Outfit), court pattern utilities, and mobile-first base styles for both light and dark modes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with pickleball color palette (green/yellow), Outfit/Inter font families, custom border radii, card shadows, and animation keyframes for smooth mobile interactions.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interaction; `CreateSessionParams` now includes `sessionType` (SessionType enum) and `isRanked` (boolean), and `useCreateSession` passes both to `actor.createSession`; exports `SessionType` alongside `GameOutcome`.",
  "frontend/src/lib/utils.ts": "Utility functions for class merging, clipboard copy, proprietary rating formatting (formatRating replacing formatDuprRating), and principal shortening.",
  "frontend/src/lib/storage.ts": "LocalStorage helpers with `principalId` made optional on `StoredPlayerProfile`, `StoredSession` updated to use `role: 'host' | 'player'` with a backward-compatible `isHost` alias, and `getSessionPlayerNames`/`setSessionPlayerName` helpers restored.",
  "frontend/src/components/Layout.tsx": "Mobile-optimized layout wrapper with sticky header, scrollable main, and branded footer; `AppHeader` now includes a logout button that clears the `sessionStorage` auth choice, calls `clear()` on Internet Identity, clears the React Query cache, and navigates to `/login`.",
  "frontend/src/components/SessionCodeDisplay.tsx": "Session code display component that accepts an explicit `sessionCode` prop (falling back to `sessionId`) and shows it without any length truncation, with copy and share buttons.",
  "frontend/src/components/CourtAssignmentCard.tsx": "Court assignment card with optional score entry UI (two numeric inputs + Submit button, shown when `showScoreEntry` is true for ranked sessions) and optional submitted score display (shown when `submittedTeamAScore`/`submittedTeamBScore` props are provided).",
  "frontend/src/components/WaitlistPanel.tsx": "Waitlist panel displaying queued players by name with position numbers, highlighting the current player, and an empty state; accepts resolved name objects instead of raw principals.",
  "frontend/src/components/GameScheduleInfo.tsx": "Game schedule summary card showing session time, max games per court, and total games calculated from totalMinutes/gameDurationMinutes/courts.",
  "frontend/src/components/MatchResultSubmission.tsx": "Match result card with a match format selector (Casual/Standard/Tournament/Finals, default Standard) and Team A/B win buttons; passes the selected format to the onSubmit callback alongside the GameOutcome.",
  "frontend/src/components/PlayerStatsCard.tsx": "Player stats card showing avatar, name, proprietary Rating badge (formatted via formatRating), and win/loss/win-rate statistics; exports both named and default.",
  "frontend/src/components/SessionConfigForm.tsx": "Game configuration form with a new game type selector (4 options: Random Allotment, Round Robin, Ladder League, King/Queen of the Court using icon cards) and a Ranked/Unranked toggle switch; both fields are included in `SessionConfigFormFields` and required before submission.",
  "frontend/src/pages/Home.tsx": "Home page that uses `currentSession.role === 'host'` (with backward-compat `isHost` fallback) to determine the resume destination, and shows the `sessionCode` in the active game chip.",
  "frontend/src/pages/ProfileSetup.tsx": "Profile creation page that calls `savePlayerProfile` without the now-optional `principalId` field.",
  "frontend/src/pages/ProfileView.tsx": "Profile view/edit page that calls `savePlayerProfile` without the now-optional `principalId` field.",
  "frontend/src/pages/CreateSession.tsx": "Game creation page that passes `sessionType` and `isRanked` from the form to the `useCreateSession` mutation, and displays the selected game type and ranked/unranked badge on the success screen.",
  "frontend/src/pages/JoinSession.tsx": "Join game page that accepts alphanumeric session codes up to 20 characters (no 6-digit restriction), strips non-alphanumeric input, and uses the `useJoinSession` hook which calls `joinSessionByCode` on the backend.",
  "frontend/src/pages/HostSessionDashboard.tsx": "Host dashboard that displays game type label and ranked/unranked badge in the header and a dedicated info card; for ranked sessions shows score entry on each court card, tracks submitted scores per court, blocks \"End Round\" until all courts have submitted scores, and shows a reminder banner with submission progress.",
  "frontend/src/pages/PlayerSessionView.tsx": "Player session view that displays the game type (with icon) and ranked/unranked badge prominently; hides the match result submission form for ranked games and instead shows an informational card explaining that the host submits scores and ratings will be updated.",
  "frontend/src/App.tsx": "Main app router with 8 routes; imports `JoinSession` as a default export (matching the updated page file).",
  "frontend/src/components/AllRoundsSchedule.tsx": "Schedule accordion component rendering all rounds with court assignments and waitlists; fixed the `CourtAssignmentCard` prop from `currentPlayerName` to `currentPlayer` to match the updated component interface.",
  "frontend/src/pages/Login.tsx": "New Login page with Internet Identity sign-in and \"Continue as Guest\" options, featuring the pickleball court background, app logo, and two action cards; exports helper functions (`setAuthChoice`, `getAuthChoice`, `clearAuthChoice`) for managing the session-scoped auth choice flag in `sessionStorage`.",
  "frontend/src/lib/errorHandling.ts": "Error handling utilities with `isCanisterStoppedError`, `isAuthError`, `isNetworkError`, `formatErrorMessage`, and `retryWithBackoff`; the `formatErrorMessage` function no longer converts auth errors into \"You need to be logged in\" messages, allowing anonymous/guest users to create and host games without being blocked by that error.",
  "frontend/src/lib/scheduler.ts": "New client-side fair round-robin scheduler that generates proper per-round court assignments and waitlists using player indices, ensuring waitlist players get priority in the next round and every player gets equal playing time across all rounds."
}
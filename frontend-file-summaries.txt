{
  "frontend/src/index.css": "Global styles with the original sporty pickleball green/yellow OKLCH color theme, custom Outfit/Inter fonts, court pattern utilities, and mobile-first base styles for both light and dark modes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with the original pickleball color palette (court greens, ball yellows), Outfit/Inter font families, custom border radii, card/court/ball shadows, and animation keyframes.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interaction; `useHostAddPlayer` now returns typed `HostAddPlayerSuccess`/`HostAddPlayerError` union types, properly maps all `AddPlayerResult` variants via `__kind__` discriminant, and invalidates the session query cache on success. Exported `HostAddPlayerParams` and `HostAddPlayerError` types allow the dashboard to handle errors with user-friendly messages.",
  "frontend/src/lib/utils.ts": "Utility functions for class merging, clipboard copy, proprietary rating formatting (formatRating replacing formatDuprRating), and principal shortening.",
  "frontend/src/lib/storage.ts": "LocalStorage helpers for player profiles and sessions; `LocalPlayerProfile` includes `name`, `mobileNumber`, `bio`, `profilePicture`, and `workField` fields; `clearCurrentSession` takes no arguments; exports backward-compat aliases.",
  "frontend/src/components/Layout.tsx": "App layout with sticky header showing Rally branding, PlayerSearchBar for authenticated users, nav icons, and footer; `flex-shrink-0` updated to `shrink-0`.",
  "frontend/src/components/SessionCodeDisplay.tsx": "Session code display component that accepts an explicit `sessionCode` prop (falling back to `sessionId`) and shows it without any length truncation, with copy and share buttons.",
  "frontend/src/components/CourtAssignmentCard.tsx": "Court assignment card showing Team A vs Team B with player chips displaying names and usernames; all `flex-shrink-0` updated to `shrink-0`.",
  "frontend/src/components/WaitlistPanel.tsx": "Waitlist panel showing players waiting for the next round with username display and guest badges; `flex-shrink-0` updated to `shrink-0`.",
  "frontend/src/components/GameScheduleInfo.tsx": "Game schedule summary card showing total minutes, courts, and total games calculated from `totalMinutes`/`gameDurationMinutes`/`courts`; was previously an empty file.",
  "frontend/src/components/MatchResultSubmission.tsx": "Match result card with format selector and Team A/B win buttons; accepts `court: bigint` and optional `guestPlayerIds` to render guest names with Ghost icons and amber styling.",
  "frontend/src/components/PlayerStatsCard.tsx": "Player stats card showing avatar, name, proprietary Rating badge (formatted via formatRating), and win/loss/win-rate statistics; exports both named and default.",
  "frontend/src/components/SessionConfigForm.tsx": "Game configuration form with a new game type selector (4 options: Random Allotment, Round Robin, Ladder League, King/Queen of the Court using icon cards) and a Ranked/Unranked toggle switch; both fields are included in `SessionConfigFormFields` and required before submission.",
  "frontend/src/pages/Home.tsx": "Home page rebranded to \"Rally\" with Zap icon, Host/Join action cards for all users, quick links for authenticated users, and guest sign-in prompt.",
  "frontend/src/pages/ProfileSetup.tsx": "Profile creation page for authenticated and guest users; fixes `undefined` → `null` for optional fields passed to `useCreateGuestProfile` mutation, and updates branding to \"Rally\".",
  "frontend/src/pages/ProfileView.tsx": "Profile view/edit page that removes the broken `getAuthChoice` import from Login and instead derives guest status directly from `identity`, with full view and edit modes.",
  "frontend/src/pages/CreateSession.tsx": "Game creation page with session type selector, ranked toggle, and auto-generated session code; host is automatically added as first player by the backend.",
  "frontend/src/pages/JoinSession.tsx": "Join game page that uses the unwrapped `string` sessionId returned by the fixed `useJoinSession` mutation, saves session with proper `StoredSession` object, and navigates correctly.",
  "frontend/src/pages/HostSessionDashboard.tsx": "Host session dashboard with Players/Rounds/Rankings tabs; uses the scheduler's `Round` type (with `teamA`/`teamB` string arrays) instead of the backend `CourtAssignment` type, passes all required args to `generateRounds` (including `totalRounds` and `sessionType`), and calls `calculateRankings` with the correct `(string[], MatchScore[][])` signature. The `AddPlayerDialog` sub-component handles both guest and registered player addition with proper error handling via `useHostAddPlayer`.",
  "frontend/src/pages/PlayerSessionView.tsx": "Player-facing session view with round navigation, court assignments showing player usernames, and schedule tab; `flex-shrink-0` updated to `shrink-0`.",
  "frontend/src/App.tsx": "Main app router with 12 routes; all routes (including create-session and host dashboard) are accessible without authentication guards, allowing guest users to host and manage sessions.",
  "frontend/src/components/AllRoundsSchedule.tsx": "Schedule accordion component rendering all rounds using the new Round type from scheduler.ts, highlighting the current round.",
  "frontend/src/pages/Login.tsx": "Login page rebranded to \"Rally\" with Zap icon, Internet Identity sign-in and guest continue options.",
  "frontend/src/lib/errorHandling.ts": "Error handling utilities with `formatAddPlayerError` (takes `AddPlayerResult`), `formatAddPlayerErrorKind` (takes a string kind for use after mutation throws), `isAddPlayerSuccess`, and existing `formatEndSessionError`/`isEndSessionNotHostError`/`isEndSessionAlreadyEndedError` helpers.",
  "frontend/src/lib/scheduler.ts": "Client-side round scheduler generating rounds with ladder league and king/queen score-based reordering, plus ranking calculation utilities.",
  "frontend/index.html": "HTML entry point with updated title \"Rally\".",
  "frontend/src/components/LoadingGame.tsx": "Animated pickleball mini-game loading screen using Canvas API with two AI-controlled paddles and a bouncing pickleball, shown during loading states.",
  "frontend/src/pages/PublicProfile.tsx": "Public player profile page using the correct route param `principalId` (from `/player/$principalId`) and navigating to `/messages/$principalId` with the correct param name.",
  "frontend/src/pages/GameHistory.tsx": "Game history page that correctly uses `CompletedMatch[]` directly from the fixed `useGetMatchHistory` hook (no `.matches` unwrap needed at call site), with `shrink-0` replacing `flex-shrink-0`.",
  "frontend/src/pages/MessagesInbox.tsx": "Messages inbox page using the correct route path `/messages/$principalId` and param name `principalId` when navigating to conversation threads.",
  "frontend/src/pages/ConversationThread.tsx": "Full conversation thread page using the correct route param `principalId` (from `/messages/$principalId`) and the restored `useGetConversation`/`useSendMessage` hooks.",
  "frontend/src/components/PlayerSearchBar.tsx": "Search bar component for the header that searches players by name/username with debounced queries and a dropdown showing View Profile and Message actions; `flex-shrink-0` updated to `shrink-0`.",
  "frontend/src/components/ScoreInputForm.tsx": "Score input form for entering team scores per court per round with validation and submit state; `flex-shrink-0` updated to `shrink-0`."
}
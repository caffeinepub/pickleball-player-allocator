{
  "frontend/src/index.css": "Global styles with a sporty green/yellow pickleball theme using OKLCH color tokens, custom fonts (Inter + Outfit), court pattern utilities, and mobile-first base styles for both light and dark modes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with pickleball color palette (green/yellow), Outfit/Inter font families, custom border radii, card shadows, and animation keyframes for smooth mobile interactions.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations; `useCreatePlayerProfile` now invalidates `currentUserProfile` on success, and `useSaveCallerUserProfile` is the primary hook used by ProfileSetup for idempotent profile saves.",
  "frontend/src/lib/utils.ts": "Utility functions for class merging, clipboard copy, DUPR rating formatting (formatDuprRating), and principal shortening; skill level and max games utilities removed.",
  "frontend/src/lib/storage.ts": "LocalStorage helpers for player profile, current session, and a session-scoped player name registry (indexed by player position) to persist names added by the host across renders.",
  "frontend/src/components/Layout.tsx": "Mobile-optimized layout wrapper with sticky header, scrollable main, and branded footer; `AppHeader` now includes a logout button that clears the `sessionStorage` auth choice, calls `clear()` on Internet Identity, clears the React Query cache, and navigates to `/login`.",
  "frontend/src/components/SessionCodeDisplay.tsx": "Reusable session code display card with large typography, copy-to-clipboard button, and native share API support with toast feedback.",
  "frontend/src/components/CourtAssignmentCard.tsx": "Court assignment card accepting pre-resolved player name strings, showing Team A vs Team B slots with avatar initials, highlight for current player, and empty slot indicators.",
  "frontend/src/components/WaitlistPanel.tsx": "Waitlist panel displaying queued players by name with position numbers, highlighting the current player, and an empty state; accepts resolved name objects instead of raw principals.",
  "frontend/src/components/GameScheduleInfo.tsx": "Game schedule summary card showing session time, max games per court, and total games calculated from totalMinutes/gameDurationMinutes/courts.",
  "frontend/src/components/MatchResultSubmission.tsx": "Match result card with Team A/B win buttons; refactored to accept `onSubmit`/`isLoading`/`onSkip` props and imports `GameOutcome` as a value directly from `../backend` (not re-exported as type).",
  "frontend/src/components/PlayerStatsCard.tsx": "Player stats card showing avatar, name, DUPR rating badge (formatted via formatDuprRating), and win/loss/win-rate statistics; exports both named and default.",
  "frontend/src/components/SessionConfigForm.tsx": "Simplified session configuration form with only a courts stepper (1-10) and submit button; allocation mode and time inputs removed.",
  "frontend/src/pages/Home.tsx": "Home page with hero banner, active session resume card, profile card showing DUPR rating, and Host/Join session action buttons with updated how-it-works guide; uses new storage function names.",
  "frontend/src/pages/ProfileSetup.tsx": "Profile creation page that now uses `useSaveCallerUserProfile` (instead of `useCreatePlayerProfile`) to save the user's name and optional DUPR rating via `saveCallerUserProfile`, then persists to localStorage and navigates to the next route.",
  "frontend/src/pages/ProfileView.tsx": "Profile view/edit page; fixes `null` → `undefined` for `duprRating` when saving profile to match `StoredPlayerProfile` type.",
  "frontend/src/pages/CreateSession.tsx": "Session creation page that properly destructures the `SessionCreationResult` returned by `createSession.mutateAsync`, extracting `result.sessionId` before persisting to localStorage and navigating to the host dashboard; includes improved error logging with specific error messages.",
  "frontend/src/pages/JoinSession.tsx": "Join session page with session code input, error handling, and navigation to player session view; updated to pass sessionId as object to useJoinSession and use setCurrentSession.",
  "frontend/src/pages/HostSessionDashboard.tsx": "Host dashboard with add-player form, allocate/rotate buttons, and tabbed current/players/schedule views; uses named imports for `Layout` and `SessionCodeDisplay`, default imports for card components.",
  "frontend/src/pages/PlayerSessionView.tsx": "Player session view showing court assignment, waitlist, match result submission, and all-courts overview; uses named `Layout` import and updated `MatchResultSubmission` props interface.",
  "frontend/src/App.tsx": "Main app router with 8 routes; adds a `/login` route (no guard) and applies a `beforeLoad` guard to all other routes that redirects unauthenticated/unchoiced users to `/login` via `sessionStorage` check.",
  "frontend/src/components/AllRoundsSchedule.tsx": "New collapsible accordion component displaying all planned rounds with court assignments and waitlist sections per round, using CourtAssignmentCard for each court and highlighting the current round.",
  "frontend/src/pages/Login.tsx": "New Login page with Internet Identity sign-in and \"Continue as Guest\" options, featuring the pickleball court background, app logo, and two action cards; exports helper functions (`setAuthChoice`, `getAuthChoice`, `clearAuthChoice`) for managing the session-scoped auth choice flag in `sessionStorage`."
}